{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<div class="row">
    {% for livre in livres  %}


    <div class="col-4 mt-3">
        <div class="card">
        <img src={{ livre.image }} alt="image">
            <div class="card-body">
                <h4 class="card-title">{{ livre.libelle}} ({{ livre.prix}} DT)</h4>
                <H5>{{ livre.categorie.libelle }}</H5>
                <p class="card-text">
                    Editeur: {{ livre.editeur }}<br>
                    Date Edition : {{ livre.dateEdition|date('d-m-Y') }}
                </p>
                <a href="#" class="btn btn-success btn-sm">
                    <i class="fas fa-shopping-cart"></i> Ajouter au panier</a>
                <a href="#" class="btn btn-primary btn-sm">Voir détail...</a>


            </div>
        </div>

        
    </div>

    {% endfor %}
</div>
    

{% endblock %}
